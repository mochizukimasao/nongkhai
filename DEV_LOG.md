# Development Log

プロジェクトの修正履歴、仮説、検証結果、および改善案を記録します。

## [2025-11-25] モバイルツールバーの無反応問題とメモリリークの修正

### 1. 問題の概要
モバイル版（特にタッチデバイス）において、ツールバーを連続して使用するとボタンが反応しなくなる。また、動作が徐々に重くなる傾向がある。

### 2. 仮説 (Hypotheses)

#### 仮説A: スクロール検出フラグの固着
- **詳細**: ツールバーの誤操作防止のための `isToolbarScrollGesture` フラグが、特定の条件下（例: `touchcancel` イベント発生時や、高速な操作時）で `true` のままリセットされず、ボタンクリック（`handleAction`）を永続的にブロックしている可能性がある。
- **根拠**: スクロール自体は機能するが、ボタンだけが反応しないという症状。

#### 仮説B: AudioContextによるメモリリーク
- **詳細**: ボタンクリック時の効果音再生 (`playSound`) において、`AudioContext` の `OscillatorNode` と `GainNode` を生成しているが、これらを明示的に切断（disconnect）していない。
- **根拠**: 連続使用で徐々に重くなる症状。Web Audio APIではノードのクリーンアップを忘れるとメモリリークの主要因となる。

#### 仮説C: 過剰なスクロール検出ロジックによるブロッキング
- **詳細**: ツールバー全体を監視する `isToolbarScrollGesture` ロジックが、ボタンのタップを「スクロール中の誤操作」と誤判定してブロックしている、または判定待ちのタイムアウトにより反応が遅れている。
- **根拠**: ユーザーからの「ネイティブアプリのようなスムーズさがない」「エラーは起きないが反応しない」というフィードバック。

### 3. 実施した修正 (Fixes Executed)

#### 修正A: スクロールフラグの安全性向上 (`js/app.js`)
（前回の修正 - 効果不十分）

#### 修正B: オーディオノードのクリーンアップ (`js/app.js`)
（前回の修正 - メモリリーク対策として有効）

#### 仮説D: カスタムタッチ処理とネイティブスクロールの競合
- **詳細**: `attachTouchAction` によるカスタムタッチ判定が、ブラウザのネイティブなスクロール処理と競合し、特にスクロール端でのバウンスや慣性スクロール時に「スタック」する現象を引き起こしている。
- **根拠**: ユーザーからの「右端までいくと左に戻れない」という報告。これはJavaScriptでタッチイベントを過剰に制御しようとした際によくある副作用。

### 3. 実施した修正 (Fixes Executed)

#### 修正A: スクロールフラグの安全性向上 (`js/app.js`)
（前回の修正 - 効果不十分）

#### 修正B: オーディオノードのクリーンアップ (`js/app.js`)
（前回の修正 - メモリリーク対策として有効）

#### 修正C: グローバルスクロール検出の削除とタッチ判定の局所化 (`js/app.js`)
（前回の修正 - 効果不十分、新たな副作用発生）

#### 修正D: ネイティブイベントへの完全移行 (`js/app.js`, `index.html`)
1.  **カスタムタッチ処理の廃止**:
    -   `attachTouchAction` 関数を完全に削除。
    -   `touchstart`, `touchmove`, `touchend` イベントリスナーを全て削除。
2.  **標準 `click` イベントの採用**:
    -   全てのボタンアクションを標準の `click` イベントで発火するように変更。
    -   エディタのフォーカス維持のため、`mousedown` イベントで `preventDefault()` を実行する処理は維持。
3.  **CSS `touch-action` の適用**:
    -   ツールバーに `touch-action: pan-x` を適用し、ブラウザに水平スクロール領域であることを明示。
    -   ボタンに `touch-action: manipulation` を適用し、ダブルタップズームなどを無効化して反応速度を向上。

### 4. 検証手順 (Verification Steps)

1.  **スクロール干渉テスト**:
    -   モバイル端末でツールバーを激しくスクロールし、直後にボタンを押す。
    -   **期待値**: ボタンが即座に反応すること。
2.  **メモリリークテスト**:
    -   Chrome DevToolsのMemoryタブでヒープスナップショットを取得。
    -   ボタンを50回連打。
    -   再度スナップショットを取得。
    -   **期待値**: AudioNodeの数が累積していないこと。

### 5. 今後の改善案 (Future Improvements)

-   **コード構造の改善**: `app.js` が1200行を超えており、機能ごとのモジュール分割（例: `audio.js`, `toolbar.js`, `editor.js`）を検討すべき。
-   **オーディオライブラリの導入**: Web Audio APIの生操作は複雑になりがちなので、より堅牢な管理のために軽量なライブラリ（Howler.jsなど）の導入を検討しても良いかもしれない（ただし、現在の要件ではオーバースペックの可能性もあり）。
-   **状態管理**: 現在、状態がグローバル変数に散らばっているため、簡単なステート管理パターンを導入するとデバッグが容易になる。
